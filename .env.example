# Supabase - apps/web
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Supabase server-side (Edge Functions / scripts only — never expose to client)
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_DB_URL=
SUPABASE_DB_PASSWORD=
SUPABASE_PROJECT_REF=
SUPABASE_ACCESS_TOKEN=

# Baniq Pay gateway (Edge Function secrets — set via `supabase secrets set`,
# values come from the Baniq dashboard; see docs/baniq-pay.md)
BANIQ_API_KEY_ID=
BANIQ_API_SECRET=
BANIQ_WEBHOOK_SECRET=
APP_BASE_URL=